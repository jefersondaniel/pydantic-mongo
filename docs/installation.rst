Installation
============

To install pydantic-mongo, simply use pip:

.. code-block:: bash

    pip install pydantic-mongo

Requirements
------------

* Python >= 3.8
* pydantic >= 2.0.2, < 3.0.0
* pymongo >= 4.9, < 5.0
